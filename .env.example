# 复制为 .env 后按需修改（docker compose 自动读取）

# 宿主访问端口
PORT=5174

# 时区（影响容器内每日备份时刻与日志时间；默认 Asia/Shanghai，即凌晨 3 点备份）
# TZ=Asia/Shanghai

# 额外放行的 Host（逗号分隔主机名），用于通过 NAS 主机名/域名访问时避免 403
# EXTRA_ALLOWED_HOSTS=nas.local,renovation-expenses.example.com

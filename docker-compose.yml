name: renovation-expenses

services:
  app:
    image: ghcr.io/baoxinwen/renovation-expenses:latest
    build: .
    container_name: renovation-expenses
    restart: unless-stopped
    ports:
      - "${PORT:-5174}:5174"
    environment:
      - NODE_ENV=production
      # 时区：README 承诺「每天 03:00 备份」，容器默认 UTC 会变成北京时间 11:00 执行
      - TZ=${TZ:-Asia/Shanghai}
      - SQLITE_JOURNAL_MODE=DELETE
      # .env 里的变量只用于本文件插值，不会自动进容器——README 承诺的
      # EXTRA_ALLOWED_HOSTS（NAS 主机名放行）必须在此显式透传
      - EXTRA_ALLOWED_HOSTS=${EXTRA_ALLOWED_HOSTS:-}
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
    # 健康检查用镜像内置定义（Dockerfile，随 PORT 环境变量联动），此处不再重复声明以免漂移
